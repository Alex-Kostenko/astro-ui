---
// import { cover } from "@assets/index";

interface Props {
  image: string;
}

const { image } = Astro.props;
---

<img
  class="w-full h-[486px] object-cover object-center"
  src={image}
  alt="project cover"
/>
