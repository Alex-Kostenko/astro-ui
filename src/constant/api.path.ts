export enum ApiPath {
  partner = "/partners",
  project = "/projects",
  projectDomain = "/project-domains",
  technology = "/technologies",
  comment = "/comments",
  contactUs = "/contact-uses",
  upload = "/upload",
}
